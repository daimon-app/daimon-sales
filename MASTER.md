# DAIMON SALES — MASTER

> このファイルは `daimon-app/daimon-sales` の**正本（Single Source of Truth）**。
> AI・人間とも、作業開始時はまずこのファイルを確認する。
> 過去の会話・古いZIP・旧仕様と矛盾した場合は、原則としてこのMASTERの最新記載を優先する。
> ただし、ユーザー（中山鉄兵）の最新の明示指示が最優先。

- 正本化開始日: 2026-08-14
- 最終同期: 2026-08-14 / v7 name-textpack
- Repository: `daimon-app/daimon-sales`
- Branch: `main`
- Status: 販売版開発中

## 1. 製品の目的

DAIMON販売版は、朝・仕事・夜などのモードを通じて、自分の状態を整え、望む方向へ戻るための自己実現サポートアプリ。

DAIMONの中核思想は「継続」より**戻ること**。
人はズレることを前提にし、ズレに気づき、罪悪感なく意図した行動・目的へ戻れる設計を優先する。

## 2. 製品として目指す基本構造

- 朝モード
- 仕事モード
- 夜モード
- 逆境モード
- PWAとして運用
- 画面はスマートフォン縦向きを基本とする

※上記は製品構成。現行v7で実装済みかどうかは「4. 現行v7実装状態」を正とする。

## 3. UXの基本原則

- 考えさせすぎない
- 操作摩擦を減らす
- 心理学・行動経済学・神経科学・人間OSの観点をUX/UIへ反映する
- 連続記録を成功指標の中心にしない
- 「戻れた」を成功として扱う
- ダークテーマでも可読性・操作性を落とさない

## 4. 現行v7実装状態

2026-08-14、`zero/import-v7-name-textpack` のコミット `45b9b2ef08b278439cf6ce6432fbe046bf4579d6` を `main` にfast-forward反映済み。

### ファイル配置

- `MASTER.md`
- `index.html`
- `assets/images.js`
- `manifest.json`
- `sw.js`
- `icons/icon-192.png`
- `icons/icon-512.png`

アイコン実体は `icons/` 配下へ移動済みで、`manifest.json` の参照先との不整合は解消済み。

### 朝モード

- 現行v7の主実装。
- 呼吸導入 → NAMEレイヤー → 朝12枚 → 締め → 現実へ戻る → ホーム、の導線を持つ。
- 名前が設定されている場合のみ独立NAMEレイヤーを表示する。
- 名前未設定時はNAMEレイヤーを自動スキップする。
- 名前はアファメーション本文へ差し込まない。
- 名前を音声で読ませない。
- 朝スライド本文は `languagePacks.ja.morning.slides` 構造へ再編済み。
- 後方互換エイリアスを保持する。
- v7移行時、朝スライド本文は逐語変更しない。

### 仕事・夜・逆境

- 製品として必要なモードだが、現行v7を基準に実装状態を都度コード確認してから変更する。
- 過去ZIP・過去会話に存在した実装を、現行mainに存在するものとして扱わない。
- 次の統合作業では、現行mainを壊さず必要機能を段階的に追加する。

### 音声

- 現行mainを基準に、各モードの音声・停止UIおよびバイノーラルビートは実コード確認後に実装・修正する。
- 過去版で動作していたことだけを根拠に「実装済み」と判定しない。

### 画像データの注意点

`assets/images.js` はv7で配置されているが、現時点では `index.html` から `<script src>` / `fetch` 等で参照されていない。
画像データは `index.html` 側にもインライン保持されており、`assets/images.js` は現状未使用の重複ファイル。

この重複は確認済み技術課題として扱う。ユーザー承認なしに画像構造を大規模変更しない。

## 5. 直近の開発焦点

販売版完成に向け、現行mainを基準に以下を優先する。

- 仕事モードの完成
- 夜モードの完成
- 逆境モードの統合／全画面サイズ・レイアウト確認
- 逆境モードのバイノーラルビート再生確認
- 朝／仕事／夜／逆境の音声・停止UI
- 戻る／ホーム導線
- 各モードの文字被り・画面下切れ・レイヤー配置
- `assets/images.js` と `index.html` 内インライン画像の重複整理方針を決める
- NAMEレイヤー仕様を維持する

## 6. NAMEレイヤー固定仕様

- 名前表示は本文とは独立したレイヤー。
- テンプレートはNAMEレイヤー内だけで生成する。
- 名前未設定時は名前行／NAMEレイヤーを表示しない。
- 名前をアファメーション本文へ差し込まない。
- 名前を音声で読ませない。
- NAME導入によって既存朝本文を改変しない。

## 7. 正本運用ルール

1. 作業開始時に `MASTER.md` と現行 `main` を読む。
2. 新しい仕様をユーザーが確定したらMASTERへ反映する。
3. 仮説・提案・未承認案はMASTERへ確定事項として書かない。
4. 古い仕様を復活させない。
5. コード変更と仕様変更を混同しない。
6. 完成判定前に、実装とMASTERの一致を確認する。
7. ZIPを取り込む場合、正本mainとの差分を確認してから別ブランチへ取り込み、検証後にmainへ昇格する。
8. mainへの反映は原則として履歴を壊さないfast-forwardまたは通常のレビュー済み変更とし、安易なforce updateを行わない。
9. 大きな履歴は将来 `DECISIONS.md` 等へ分離し、MASTERは「現在の正解」を短く保つ。

## 8. AIチーム運用

- ゼロ（ChatGPT）: 設計・判断・仕様確定・全体監督・GitHub正本管理
- Codex: 実装・コード修正・テスト
- Claude（クロちゃん）: 大規模実装・複数ファイル改修・完成ZIP
- Gemini: 調査・候補出し・比較（重要事項は一次情報で裏取り）
- Manus: LP・販売ページ・見せ方・売り場
- NotebookLM: 確定資料・大量資料の分析／保管補助
- GitHub: コードおよび確定仕様の正本

## 9. 更新原則

このMASTERは完成文書ではなく、DAIMON販売版の進行に合わせて更新する。

**最優先順位:**

`ユーザーの最新明示指示 > MASTER最新記載 > 現行mainコード > 過去会話・旧ZIP・旧仕様`

## 10. v8 共通Engine＋4 Mode Pack（2026-08-20）

- `languagePacks.ja` 配下を `morning.slides` / `work.slides` / `night.slides` の3 Pack構成とする。
- 朝・仕事・夜・逆境は、生成・進行・自動送り・スワイプ・③演出・fit・停止を同じEngineで処理する。
- 朝v7の12文章、12画像dataURI、名前レイヤー、演出と導線は維持する。
- 仕事・夜は各12枚の確定文章と `audio:null` を保持する。
- 仕事画像は `work01`〜`work12`、夜画像は `night01`〜`night12` をインライン画像辞書の受け口とし、個別素材確定までは合成一覧画像や仮画像を入れない。
- 夜は同じEngineの時間係数だけを遅くし、低刺激のテンポにする。
- 逆境は既存の正式20画面本文と周波数データを共通Engineへ統合し、左右200Hz基準のバイノーラル音響を音声ON/OFF・停止・Homeと連動させる。
- 言語選択UI、TTS、全文自由編集は実装しない。
