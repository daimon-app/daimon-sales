services:
  billing-verifier:
    build: .
    env_file: .env
    ports: ["127.0.0.1:8080:8080"]
    volumes: ["./data:/app/data"]
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
