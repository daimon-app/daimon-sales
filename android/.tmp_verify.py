# Scratch file used once during this session to statically cross-check
# test-readiness.ps1's assertions (Bash's `rm` required an approval this
# session could not obtain, so it could not be deleted). Not part of the
# Android project; safe to delete.
